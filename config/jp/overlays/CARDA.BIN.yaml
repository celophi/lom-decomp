# Auto-generated by tools/versions/bootstrap_jp_configs.py (first pass).
# Coarse layout: rodata / one asm block / data. Refine by hand, then drop
# this note.
name: CARDA.BIN
sha1: 22615b343b5e34973a5184732972a75dfd4d15fc
options:
  base_path: ../../../
  basename: CARDA.BIN
  platform: psx
  compiler: PSYQ
  # include/include_asm.h and the .inc files are project-owned.
  generate_asm_macros_files: False
  target_path: disc/jp/BIN/CARDA.BIN
  extensions_path: tools/splat_ext
  asset_path: assets/jp
  elf_path: build/jp/overlays/carda.elf

  asm_path: asm/jp/overlays/carda
  src_path: src/overlays/carda
  build_path: build/jp/overlays/carda

  ld_script_path: linker/jp/overlays/carda/carda.ld
  ld_dependencies: True
  undefined_funcs_auto_path: linker/jp/overlays/carda/undefined_funcs_auto.txt
  undefined_syms_auto_path: linker/jp/overlays/carda/undefined_syms_auto.txt

  symbol_addrs_path:
    - config/jp/symbols/carda_symbol_addrs.txt

  find_file_boundaries: False
  make_full_disasm_for_code: True
  use_legacy_include_asm: False
  o_as_suffix: True
  subalign: 2

  section_order: [".rodata", ".text", ".data", ".sdata", ".sbss", ".bss"]

  # ASCII, not SHIFT-JIS: splat writes decoded strings into the .s files
  # as UTF-8, which reassembles to different (longer) bytes. Japanese
  # text stays as raw data so the disassembly round-trips exactly.
  string_encoding: ASCII
  data_string_encoding: ASCII

segments:
  - name: carda
    type: decompress_overlay
    start: 0x1
    vram: 0x80140000
    align: 4
    subsegments:
      - [0x1, rodata, carda_rodata]
      - [0x371, asm, carda_text]
      - [0xB00D, data, carda_data]
  - [0x2FD79]
