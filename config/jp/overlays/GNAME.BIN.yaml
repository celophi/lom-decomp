# Auto-generated by tools/versions/bootstrap_jp_configs.py (first pass).
# Coarse layout: rodata / one asm block / data. Refine by hand, then drop
# this note.
name: GNAME.BIN
sha1: 549a0cd0f66267c8b9e8683575793ba99a0f6a6d
options:
  base_path: ../../../
  basename: GNAME.BIN
  platform: psx
  compiler: PSYQ
  # include/include_asm.h and the .inc files are project-owned.
  generate_asm_macros_files: False
  target_path: disc/jp/BIN/GNAME.BIN
  extensions_path: tools/splat_ext
  asset_path: assets/jp
  elf_path: build/jp/overlays/gname.elf

  asm_path: asm/jp/overlays/gname
  src_path: src/overlays/gname
  build_path: build/jp/overlays/gname

  ld_script_path: linker/jp/overlays/gname/gname.ld
  ld_dependencies: True
  undefined_funcs_auto_path: linker/jp/overlays/gname/undefined_funcs_auto.txt
  undefined_syms_auto_path: linker/jp/overlays/gname/undefined_syms_auto.txt

  symbol_addrs_path:
    - config/jp/symbols/shared_symbol_addrs.txt
    - config/jp/symbols/gname_symbol_addrs.txt

  find_file_boundaries: False
  make_full_disasm_for_code: True
  use_legacy_include_asm: False
  o_as_suffix: True
  subalign: 2

  section_order: [".rodata", ".text", ".data", ".sdata", ".sbss", ".bss"]

  # ASCII, not SHIFT-JIS: splat writes decoded strings into the .s files
  # as UTF-8, which reassembles to different (longer) bytes. Japanese
  # text stays as raw data so the disassembly round-trips exactly.
  string_encoding: ASCII
  data_string_encoding: ASCII

segments:
  - name: gname
    type: decompress_overlay
    start: 0x1
    vram: 0x80140000
    align: 4
    subsegments:
      - [0x1, rodata, gname_rodata]
      - [0x5, asm, gname_text]
      - [0x2C6D, data, gname_data]
  - [0xFE45]
