# C translation units that the JP build takes from splat assembly instead of C,
# because their code or data differs from the US release. mk/version.mk
# excludes them from the JP C build. Generated by tools/versions/port_us_layout.py
# from config/jp/ yamls: switch a unit's subsegments back to C there to drop it.
src/overlays/menu/menu_screens.c
src/overlays/shop/shop_render.c
