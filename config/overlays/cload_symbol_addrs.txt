// Field dialogue/text renderer exports used by this overlay.
field_text_reset_windows             = 0x800643E0;
field_text_reset_scratch             = 0x8006441C;

// Second Shift-JIS conversion table (7 blocks of [16][33] bytes) used by
// func_80145D28 for lead bytes 0x19..0x1F. The C indexes it with a -0x19 bias,
// which gcc folds into the base address; splat therefore renders the reference
// as `func_80143324 + 0x2C` (0x80143350) and cannot name it on its own, so the
// symbol has to be declared here or the overlay will not link.
D_801466E0                           = 0x801466E0;
