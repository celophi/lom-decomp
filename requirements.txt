# requirements-dev.txt
# Development dependencies for Legend of Mana PS1 decomp
# Install with: pip install -r requirements-dev.txt

# splat from local submodule (editable mode) with MIPS/PS1 support
-e tools/splat[mips]

# PyYAML for objdiff config generation
PyYAML>=6.0

# Required by decomp-permuter
pycparser==2.23
toml==0.10.2

# m2c MIPS-to-C decompiler
-e tools/m2c
graphviz>=0.20.1